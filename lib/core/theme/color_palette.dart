import 'package:flutter/material.dart';

class AppColors {
  // Light mode colors
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightBackground2 = Color(0xFFFFFFFF);
  static const lightPrimary = Color(0xFF1DCD9F);
  static const lightSecondary = Color(0xFF169976);
  static const lightText = Color(0xFF000000);

  // Dark mode colors
  static const darkBackground = Color(0xFF000000);
  static const darkBackground2 = Color(0xFF000000);
  static const darkPrimary = Color(0xFF1DCD9F);
  static const darkSecondary = Color(0xFF169976);
  static const darkText = Color(0xFFFFFFFF);
}
